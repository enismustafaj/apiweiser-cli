// Mimics @sindresorhus/is's `Assert` shape: a type alias to an anonymous
// object literal (not an interface/class) - see Scanner.typeName's comment
// for why that matters.
type Assert = {
  boolean(value: unknown): void;
};

export declare const assert: Assert;
