// Mimics a real package with no bundled types (e.g. node-fetch v2), whose
// declarations live in a separate @types/<name> package instead - see
// Scanner.dependencyOf's comment for why that needs its own path check.
export declare function ping(): string;
